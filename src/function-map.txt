Wayfinding Plugin(action, options, callback)

	makePin(x, y, type)

	buildDataStore(mapNum, map, el)

	buildPortals(maps)

	getDoorPaths(maps, door)

	recursiveSearch(segmentType, segmentFloor, segment, length)

	generateRoutes(startpoint, maps)

	backTrack(segmentType, segmentFloor, segment)

	getShortestRoute(maps, destinations, startpoint)
		_minLengthRoute(maps, destinations, startpoint)

	setStartPoint(startPoint, el)

	setEndPoint(endPoint, el)

	getOptions(el)

	setOptions(el)

	checkIds()

	cleanupSVG(el)

	initalizePanZoom(el)

	activateSVG(obj, svgDiv)

	replaceLoadScreen(el)

	establishDataStore(accessible, onReadyCallback)

	initalize(obj, callback)

	switchFloor(floor, el)

	hidePAth(obj)

	interpolateValue(oldValue, newValue, i, steps)

	animatePath(drawing, drawingSegment)

	panzoomWithViewBoxCoords(cssDiv, svg, x, y, w, h)

	routeTo(destination)

	build(startpoint, maps, accessible)

	dispatch function